# Contributing

## Convex deployment

- Use `npm run convex:deploy` to deploy. It explicitly pins production deployment `rightful-mockingbird-389`.
- On this Convex CLI version, `npx convex deploy` already deploys to production by default.
- `npx convex run` without `--prod` hits the dev deployment.
- The production deployment is `rightful-mockingbird-389`.
