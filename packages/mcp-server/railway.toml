[build]
builder = "RAILPACK"
buildCommand = "npm install && npm run build"
watchPatterns = ["packages/mcp-server/**"]

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
