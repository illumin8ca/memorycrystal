# Component Styles (Observed)

There is no UI component library in this repository.

## MCP/CLI interaction patterns

- Stable command outputs are JSON payloads and script logs.
- Plugin handler contract uses startup and post-turn hooks.
- Memory tools are declaratively described in `mcp-server/src/tools/*`.
