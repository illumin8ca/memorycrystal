[build]
builder = "nixpacks"
buildCommand = "npm run build --workspace memorycrystal-app"

[deploy]
startCommand = "npm run start --workspace memorycrystal-app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[nixpacks]
aptPkgs = []

[variables]
NODE_VERSION = "22.13.0"
